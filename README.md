
Установка

    ssh-add ~/.ssh/id_rsa
    mkdir ./code
    vagrant plugin install vagrant-hostsupdater

    vagrant up

Вход

    vagrant ssh

Код основного проекта лежит в каталоге `~/code/kiiiosk.dev`
